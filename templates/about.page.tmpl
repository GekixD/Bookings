{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="col">
            <h1>This is the about page </h1> 
            <p>and this is a paragraph v2</p>
            <p>This is from the template: {{index .StringMap "test"}}</p>
            <p>
                {{if ne (index .StringMap "remote_ip") ""}}
                    Your remote IP address is: {{index .StringMap "remote_ip"}}
                {{else}}
                    Your remote IP address is unknown, visit the <a href="/">Home page</a> to be set.
                {{end}}
            </p>
        </div>
    </div>
{{end}}